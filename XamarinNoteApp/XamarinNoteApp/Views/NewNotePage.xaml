<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinNoteApp.Views.NewNotePage">

    <ContentPage.Content>
        <StackLayout>
            <FlexLayout HeightRequest="60" Margin="10, 0" BackgroundColor="AliceBlue">
                <ImageButton
                    Command="{Binding SaveNoteCommand}"
                    Source="back.png"
                    HeightRequest="30"
                    BackgroundColor="Transparent"
                    Padding="2" />
            </FlexLayout>
            <Editor
                Placeholder="Type here..."
                HeightRequest="1000"
                Text="{Binding NewNoteText}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>